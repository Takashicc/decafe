{
  "scripts": {
    "dev": "concurrently \"yarn --cwd backend run dev\" \"yarn --cwd frontend run dev\"",
    "migrate": "yarn --cwd backend run migrate",
    "seed": "yarn --cwd backend run seed"
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  }
}
